#include <stdio.h>
#include "UserInterface.h"

/* local data type */

/* prototype for local functions */

/* functions definition */

void showWelcome(void) {
  puts("Welcome!");
}

void showMenu(int level) {
  switch (level) {
    case 1: {

      break;
    }
    default:
      break;
  }
}

/* local functions */
/* And for the record the progression is: 1st = primary, 2nd = secondary, 3rd = tertiary, 4th = quaternary, 
5th = quinary, 6th = senary, 7th = septenary, 8th = octonary, 9th = nonary, 10th = denary */
void PrimaryMenu(void) {
  puts("You have the following options: ");
  puts("1) ");
}
